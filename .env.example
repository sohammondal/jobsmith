# Firecrawl API key — required for scan-custom.mjs / scan_custom_pages
# (companies with a custom career-page ATS), optional but recommended for
# scan-hn-jobs.mjs / scan_hn_jobs (improves match accuracy on role-less
# HN Jobs titles). Get a free key (no card required) at https://firecrawl.dev
FIRECRAWL_API_KEY=
