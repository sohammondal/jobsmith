node_modules/
.env
.claude/settings.local.json
.DS_Store

# Runtime state — regenerated by the scripts, safe to lose
resolved-companies.json
seen-jobs.json
hn-checked.json
new-matches.log.tsv

# Personal source material — only the .example.md scaffolding ships;
# copy each to its real filename and fill in your own info (never committed)
profile/cv-template.md
profile/resume.md
profile/work-log.md

# Personal application content — only the README (structure doc) and the
# synthetic example folder below ship
applications/*/
!applications/example-acme-platform-engineer/
