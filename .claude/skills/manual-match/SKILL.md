---
name: manual-match
description: Log a job posting the user pasted by hand (LinkedIn, a career page with no API, anything jobsmith can't scan automatically) and prepare its application folder. Use when the user pastes full posting text (title, company, description) and asks to save/log/track it.
---

Follow `workflows/manual-match.md` at the root of this repo, exactly as
written. That file is the actual procedure — tool-agnostic on purpose, so it
also works from OpenCode or any other AI CLI — this SKILL.md just makes it
auto-invocable from Claude Code.

Read `workflows/manual-match.md` in full before doing anything.
